[project]
name = "ai-agent-tutorial"
version = "0.1.0"
description = "Learn LangChain, RESTful API, Dify, and WebHooks hands-on"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
